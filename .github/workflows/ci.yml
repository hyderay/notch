name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build app bundle
        run: make app
      - name: Verify signature
        run: codesign --verify --deep --strict .build/Notch.app
